//Problem : Given a list of integers nums, split it into two lists of equal size where the absolute difference between each list's median is as small as possible and return this difference.

int solve(vector<int>& nums) {
    sort(nums.begin(),nums.end());
    int n = nums.size();
    return abs(nums[n/2-1]-nums[n/2]);
}